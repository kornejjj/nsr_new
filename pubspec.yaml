name: nonstop_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.2  # Обновите версию Dart SDK

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # 🔹 Обновляем до последней совместимой версии
  firebase_core: ^3.12.0
  firebase_analytics: ^11.4.2
  firebase_auth: ^5.5.0
  cloud_firestore: ^5.6.3
  firebase_storage: ^12.4.3
  flutter_plugin_android_lifecycle: ^2.0.20


  flutter_localizations:
    sdk: flutter
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
    - assets/flag.png
    - assets/default_avatar.png
